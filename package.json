{
  "name": "event-analytics-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/dashboard"
  ],
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n SERVER,DASH -c cyan,magenta \"npm run dev --workspace=packages/server\" \"npm run dev --workspace=packages/dashboard\"",
    "build": "npm run build --workspace=packages/shared && npm run build --workspace=packages/server && npm run build --workspace=packages/dashboard",
    "lint": "npm run lint --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
